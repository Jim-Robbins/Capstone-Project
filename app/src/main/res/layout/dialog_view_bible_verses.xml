<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <RelativeLayout
        android:id="@+id/btn_add_new_passage"
        android:layout_width="match_parent"
        android:paddingStart="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@+id/recycler_view"
        android:background="@color/lime_400">

        <ImageView
            android:id="@+id/icon_circle"
            android:layout_width="@dimen/item_done_icon_width"
            android:layout_height="@dimen/item_done_icon_width"
            android:tint="@color/green_400"
            android:src="@drawable/bg_circle" />

        <ImageView
            android:layout_width="@dimen/item_done_icon_width"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_add_24dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/dialog_bible_passage_add"
            android:layout_centerVertical="true"
            android:layout_marginStart="8dp"
            android:layout_toEndOf="@id/icon_circle"/>
    </RelativeLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:scrollbars="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btn_add_new_passage"
        />

</RelativeLayout>